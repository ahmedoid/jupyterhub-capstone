# The per-student single-user image: the scientific-Python stack + LangChain baked in.
# Students bring their OWN API keys for the capstone (set in their own notebook env).
FROM quay.io/jupyter/scipy-notebook:latest

RUN pip install --no-cache-dir \
      langchain \
      langchain-openai \
      langgraph
